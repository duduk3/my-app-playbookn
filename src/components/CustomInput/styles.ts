import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    width: '100%',

    borderColor: '#bbb',
    borderWidth: 1,
    borderRadius: 5,
    
    padding: 10,
    marginBottom: 10,
  },
  input: {

  },
})